plugins {
	id 'java-library'
	id 'kotlin'
}
apply from: "$rootDir/env/secrets.gradle"

//tasks.findByName("compileKotlin").dependsOn(tasks.getByName("spotlessCheck"))

java {
	sourceCompatibility = JavaVersion.VERSION_1_7
	targetCompatibility = JavaVersion.VERSION_1_7
}
